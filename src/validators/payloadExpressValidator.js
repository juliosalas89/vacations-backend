// import { validationResult } from "express-validator";

// const payloadExpressValidator = (req, res, next, config) => {
//     const errors = validationResult(req);
//     if(!errors.isEmpty()) {
//         const badRequest = errors.array().find(val => )
//     }

// }